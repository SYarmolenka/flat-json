const LEVEL_DIVIDE = '$';
const OBJECT_DIVIDE = '.';

const ARRAY = 'Array';
const OBJECT = 'Object';
const STRING = 'String';
const NUMBER = 'Number';
const BOOLEAN = 'Boolean';
const NULL = 'Null';

const EMPTY_ARRAY = 'EmptyArray';
const EMPTY_OBJECT = 'EmptyObject';

module.exports = {
  LEVEL_DIVIDE,
  OBJECT_DIVIDE,
  ARRAY,
  OBJECT,
  STRING,
  NUMBER,
  BOOLEAN,
  NULL,
  EMPTY_ARRAY,
  EMPTY_OBJECT,
};
